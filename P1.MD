# P1 ANLATIMLAR

```bash
# 🔹 Vagrant

# Sanal makineleri (VM) otomatik kuran araç.

# Vagrantfile ile VM’lerin CPU, RAM, IP gibi ayarları kodla yazılır.

vagrant up # → VM açılır, vagrant ssh → içine girilir.
```

```bash
# 🔹 VirtualBox

#Vagrant’ın kullandığı sanallaştırma programı.

# VM’ler aslında VirtualBox üzerinde çalışır.
```

```bash
# 🔹 VM (Virtual Machine)

# Bilgisayar içinde çalışan “sanal bilgisayar”.

# IoT projesinde her VM bir node görevi görür.
```

```bash
# 🔹 Node

# Kubernetes cluster’ındaki makine.

# Server/Master node → cluster’ın beyni (API server, scheduler, controller).

# Worker node → uygulamaların (Pod) çalıştığı yer.
```

```bash
# 🔹 k3s

# Kubernetes’in hafif sürümü (tek binary).

# Az kaynaklı makinelerde (512MB RAM gibi) bile çalışır.

# Normal Kubernetes’le uyumludur ama kurulumu çok daha basittir.
```

```bash
# 🔹 Node

# Kubernetes cluster’ındaki makine.

# Server/Master node → cluster’ın beyni (API server, scheduler, controller).

# Worker node → uygulamaların (Pod) çalıştığı yer.
```

```bash
# 🔹 Pod

# Kubernetes’te çalışan en küçük birim.

# İçinde 1 veya daha fazla container olabilir.
```

# P1 KOMUTLAR

```bash
ifconfig en0
```

```bash
hostname
```

```bash
kubectl get nodes -o wide
```

```bash
k3s --version
```
